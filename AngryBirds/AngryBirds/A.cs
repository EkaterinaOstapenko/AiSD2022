using System;
using System.Collections.Generic;
using System.Windows;
using 

namespace AngryBirds
{
     class A
    {
        Application S = Application();
        S.Run(new Window());
    }
}
